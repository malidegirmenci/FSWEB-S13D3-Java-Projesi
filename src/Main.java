import java.lang.reflect.Array;
import java.util.Arrays;

public class Main {
    public static void main(String[] args) {
        /*Person Class
            Person johnDoe = new Person("John", "Doe", 17);
            if (person.isTeen()) {
                System.out.println(person.getFirstName() + " " + person.getLastName() + " is " + person.getAge() + " year(s) old. So it is a teen");
            } else {
                System.out.println(person.getFirstName() + " " + person.getLastName() + " is " + person.getAge() + " year(s) old. So it is not a teen");
            }
        */

        /* Person Class 2
            Person aliVeli = new Person("Ali", "Veli", 30,"Teacher", new String[]{"Reading", "Chess", "Watching Movie"},183,100);

            System.out.println("Hi I'm "+person.getFirstName()+" " + person.getLastName()+",");
            System.out.println("I'm "+ person.getAge() +" years old. My height is "+ person.getHeight()+ " and my weight is "+ person.getWeight() + ". I'm working as "+ person.getProfession()+".");
            System.out.println("My hobbies are "+ Arrays.toString(person.getHobbies()));
         */

        /*Wall Class
            Wall wall = new Wall(5,4);
            System.out.println("Wall's width: "+wall.getWidth());
            System.out.println("Wall's height: "+wall.getHeight());
            System.out.println("Wall's area: "+wall.getArea());
            wall.setHeight(-1.5);
            System.out.println("Wall's height: "+wall.getHeight());
            System.out.println("Wall's area: "+wall.getArea());
        */
    }
}